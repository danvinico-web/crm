{
  "name": "mvcrm-testserver",
  "version": "1.0.0",
  "private": true,
  "description": "Local mock of the MVCRM (MyView CRM) affiliate integration API for testing LeadHub without hitting the real https://mvcrm.online.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
